/orfeus
/native/target/
/native/target-nix/
# A cargo invocation run from inside native/ resolves CARGO_TARGET_DIR one level
# deeper; ignore that too rather than committing a build tree.
/native/native/
*.fasl
*.lx64fsl
*.dx64fsl
*.dylib
*.so
*.o
/result
/result-*
.direnv/
.envrc.local
